{
  "name": "leanix-accelerate",
  "version": "1.0.6",
  "description": "Unlock new levels of efficiency and productivity with LeanIX Accelerate, the ultimate browser extension for LeanIX users.",
  "private": true,
  "scripts": {
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
